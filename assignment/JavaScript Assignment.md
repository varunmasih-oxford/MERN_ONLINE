# JavaScript Assignment  
## Control Flow & Functions

---

## Section A: Control Flow

### Q1.
Write a program that checks if a number entered by the user is **positive, negative, or zero** using `if...else`.

---

### Q2.
Create a program to check whether a person is **eligible to vote** (age ≥ 18). Use `if...else`.

---

### Q3.
Write a program that takes marks as input and prints grades using **if…else if…else**:

| Marks | Grade |
|-------|-------|
| 90+   | A     |
| 75–89 | B     |
| 50–74 | C     |
| <50   | Fail  |

---

### Q4.
Use the **ternary operator (`? :`)** to check whether a number is **even or odd**.

---

### Q5.
Write a program using **switch case** that prints the day of the week based on a number:  
`1 = Monday, 2 = Tuesday, … 7 = Sunday`.

---

### Q6.
Using a **while loop**, print numbers from **1 to 10**.

---

### Q7.
Write a **do…while loop** program that asks the user to enter a number greater than 10. Keep asking until they enter a valid number.

---

### Q8.
Use a **for loop** to print the multiplication table of **5**.

---

### Q9.
Write a program that prints numbers from **1 to 20**, but stops the loop when it reaches **15** using `break`.

---

### Q10.
Write a loop that prints numbers from **1 to 10** but skips number **5** using `continue`.

---

## Section B: JavaScript Functions

### Q11.
Write a **function** named `addNumbers` that takes two parameters and returns their sum. Call the function with different values.

---

### Q12.
Create a function `greetUser(name = "Guest")` that prints:  
**"Welcome, <name>!"**  
If no name is passed, it should greet **"Guest"**.  
(Use **default parameters**)

---

### Q13.
Write an **arrow function** that returns the square of a number.

**Example:**  
`square(4)` → `16`

---

### Q14.
Create an **object** called `calculator` with methods:

- `add(a, b)`
- `subtract(a, b)`

Call both methods and print the results. (Object Methods)

---

### Q15.
Write a function that:
- Takes a number as input  
- Uses a loop inside the function  
- Returns the **factorial** of the number  

**Example:**  
`factorial(5)` → `120`

---
